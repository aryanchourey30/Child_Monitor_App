[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "guardian-baby"
version = "0.1.0"
description = "Local-first baby safety backend"
requires-python = ">=3.11"
